document.addEventListener('DOMContentLoaded', function() {
    const addButton = document.getElementById('add-product-btn');
    addButton.addEventListener('click', () => {
        alert('Функция добавления продукта (это просто заглушка).');
    });


});
